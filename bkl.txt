public void print_offsets()
    {
        
        for (Map.Entry<String, Class> entry : classes.entrySet()) 
        {
            String _name = entry.getKey();
            Class _class = entry.getValue();
            System.err.println(_name + " has super: " + _class.superclass());
            
            _class.print_offsets(get_class(_class.superclass()));

        }
    }

    public void print_offsets(Class _super)
    {
        if (_super != null)
        {
            offset_var = _super.offset_var;
            offset_func = _super.offset_func;
        }

        for (Map.Entry<String, Variable> entry : variables.entrySet())
        {
            String name = entry.getKey();
            String type = entry.getValue().type();

            

        }

        for (Map.Entry<String, Function> entry : functions.entrySet())
            entry.getValue().print("\t");
        



    }